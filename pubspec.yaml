name: drink_tracker
description: Water and drinks tracker with achievements
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.3
  provider: ^6.1.2
  intl: ^0.19.0

flutter:
  uses-material-design: true
